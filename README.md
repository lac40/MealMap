# MealMap

Monorepo for the MealMap project.

- `api/` contains the OpenAPI specification (contract-first).
- `apps/frontend/` will hold the React + TypeScript client.
- `apps/backend/` will hold the backend (Spring Boot or Flask).
- `tools/codegen/` will hold codegen configs for generating clients and stubs.
